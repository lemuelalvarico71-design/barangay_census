import 'package:flutter/material.dart';

class CensusDataPage extends StatelessWidget {
  const CensusDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Census Data',
        style: TextStyle(fontSize: 22, fontWeight: FontWeight.bold),
      ),
    );
  }
}
